import useButtons from "./Buttons"
import useFooters from "./Footers"
import useHeaders from "./Headers"
import useLinks from "./Links"

const useComponents = () => {
  return { useHeaders, useFooters, useLinks, useButtons }
}

export default useComponents
