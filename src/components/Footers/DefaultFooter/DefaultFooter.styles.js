import styled from "styled-components"
import tw from "twin.macro"

export const StyledFooter = styled.footer.attrs({
  className: "StyledFooter",
})`
  ${tw`absolute bottom-0 w-full bg-primary-500 text-center`}
`
